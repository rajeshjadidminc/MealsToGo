import { colors } from "../theme/colors";
import { fonts, fontWeights, fontSizes } from "../theme/fonts";
import { sizes } from "../theme/sizes";
import { lineHeights, space } from "../theme/spacing";

export const theme = {
  colors,
  fonts,
  fontWeights,
  fontSizes,
  sizes,
  lineHeights,
  space,
};
